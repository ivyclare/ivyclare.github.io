---
phase: 01-skeleton-home-mvp
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified: []
autonomous: false
requirements:
  - INFRA-03
  - INFRA-04
tags:
  - static-site
  - verification
  - human-checkpoint

must_haves:
  truths:
    - "Opening `index.html` via `file://` in a modern browser renders the Home page correctly: profile photo visible, name+role displayed, bio readable, scrollable news container present with internal scrollbar, contact links clickable, no broken images, no missing CSS."
    - "Clicking the CV link opens `assets/Ivoline_Ngong_CV_August_2025.pdf` in the browser (inline or downloads — both acceptable)."
    - "Clicking the email link opens the user's default mail client with `ivolinengong@gmail.com` pre-filled."
    - "Browser developer console (when index.html is open) shows zero JavaScript errors, zero CSP violations, zero failed asset loads (404s on `assets/profile_pic.png` or `style.css` would be visible as failed network requests)."
    - "Scrolling inside the `.news-list` container scrolls the entries internally without scrolling the rest of the page."
    - "The repo would deploy as-is to GitHub Pages (no Jekyll config, no Actions workflow, no build step needed) — confirmed by static-file inspection only."
  artifacts:
    - path: ".planning/phases/01-skeleton-home-mvp/01-04-SUMMARY.md"
      provides: "Verification record proving Phase 1 success criteria are met before phase close"
  key_links: []
---

<objective>
Verify that the three prior plans in Phase 1 (01-01 scaffolding, 01-02 CSS, 01-03 HTML) compose into a working Home page. This plan is the human-verification gate for Phase 1: there is no automated way to confirm "looks right in a browser" without actually opening a browser.

Purpose: ROADMAP Phase 1 success criteria are 6 numbered items, of which #1, #2, #3, #5, and #6 require visual or interaction confirmation. Automated grep gates in Plans 01–03 caught extraction errors and missing markup, but the final proof is opening the page locally.

Output: A SUMMARY documenting the verification result (PASS/FAIL per success criterion), the browser used, and any defects discovered. If defects found, this plan's SUMMARY enumerates them so a follow-up plan (or `--gaps` invocation) can close them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-skeleton-home-mvp/01-UI-SPEC.md
@.planning/phases/01-skeleton-home-mvp/01-01-PLAN.md
@.planning/phases/01-skeleton-home-mvp/01-02-PLAN.md
@.planning/phases/01-skeleton-home-mvp/01-03-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run automated end-of-phase sanity checks and prepare browser-open command</name>
  <files>(read-only — produces only SUMMARY content)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 success criteria — 6 numbered items)
    - .planning/phases/01-skeleton-home-mvp/01-UI-SPEC.md (for the visual contract checklist)
    - The repo root listing (`ls -la`) — confirm final shape matches the target layout in UI-SPEC §File Layout for Phase 1
  </read_first>
  <action>
    Run the following automated sanity checks via Bash and record results in the SUMMARY. Each check corresponds to one or more Phase 1 success criteria.

    1. **Repo layout** — `ls -la` and `ls assets/`. Confirm presence of: `index.html`, `style.css`, `CNAME`, `assets/profile_pic.png`, `assets/Ivoline_Ngong_CV_August_2025.pdf`, `.gitignore`. Confirm absence of: root-level `profile_pic.png`, root-level `Ivoline_Ngong_CV_August_2025.pdf`.

    2. **CNAME content** — `cat CNAME | tr -d '\n'` must equal exactly `ivolinengong.com`.

    3. **Zero Wayback artifacts** — `grep -c 'web.archive.org' index.html style.css` must report `0` for both files.

    4. **Zero scripts** — `grep -c '<script' index.html` must report `0`.

    5. **Zero CDN webfonts / external CSS** — `grep -E 'fonts.googleapis|fonts.gstatic|cdn.jsdelivr|unpkg' index.html style.css` must produce no matches.

    6. **CSS link** — `grep -q 'href="style.css"' index.html` returns success.

    7. **Asset link** — `grep -q 'src="assets/profile_pic.png"' index.html` and `grep -q 'href="assets/Ivoline_Ngong_CV_August_2025.pdf"' index.html` both succeed.

    8. **Scrollable news markup** — `grep -q 'class="news-list"' index.html` and `grep -q 'tabindex="0"' index.html` succeed. `grep -c 'class="news-entry"' index.html` reports the actual entry count.

    9. **News entry count** — confirm it matches the count reported in `01-03-SUMMARY.md`.

    10. **All 5 nav items** — `grep -c 'data-todo="phase-2"' index.html` should equal `4` (Papers, Talks, Code, Writing); plus exactly one `aria-current="page"` (Main).

    11. **CSS sanity** — `grep -c -- '--accent: #0F766E' style.css` ≥ 1; `wc -l style.css` ≥ 120.

    12. **No Jekyll config** — confirm absence of `_config.yml`, `Gemfile`, `_layouts/`, `_includes/` (these would force Jekyll on GitHub Pages). `ls _config.yml Gemfile 2>&1 | grep -c "No such"` should equal `2`. INFRA-03 requires zero-config GitHub Pages serving.

    13. **No node project sneaked in** — confirm absence of `package.json`, `node_modules/`. INFRA-04 forbids build steps.

    14. **Profile photo file size warning** — `ls -la assets/profile_pic.png` — if size > 500KB, flag in SUMMARY for Phase 3 image optimization (QUAL-06 lives in Phase 3).

    15. **Open-in-browser command** — determine the local file URL: `file:///Users/ivolinengong/Documents/My_Website/index.html`. The macOS `open` command (available since this is darwin per env) will open in the default browser: `open /Users/ivolinengong/Documents/My_Website/index.html`. Record this command in the SUMMARY but do NOT execute it yet — Task 2 (the human checkpoint) drives the actual browser open.

    Record every check's PASS/FAIL plus actual value in the SUMMARY. If any automated check fails, do NOT proceed to Task 2 — instead, surface the failure to the orchestrator with the specific gate that failed and which prior plan owns the fix.
  </action>
  <verify>
    <automated>test -f index.html &amp;&amp; test -f style.css &amp;&amp; test -f CNAME &amp;&amp; test -f assets/profile_pic.png &amp;&amp; test -f assets/Ivoline_Ngong_CV_August_2025.pdf &amp;&amp; test ! -f profile_pic.png &amp;&amp; test ! -f Ivoline_Ngong_CV_August_2025.pdf &amp;&amp; test ! -f _config.yml &amp;&amp; test ! -f Gemfile &amp;&amp; test ! -f package.json &amp;&amp; ! grep -q 'web.archive.org' index.html &amp;&amp; ! grep -q '&lt;script' index.html &amp;&amp; [ "$(cat CNAME | tr -d '\n')" = "ivolinengong.com" ]</automated>
  </verify>
  <acceptance_criteria>
    - Every automated check in the action list above is run and the result (PASS / FAIL + actual value) is recorded.
    - If any check fails, the SUMMARY clearly identifies which check, which Phase 1 success criterion it maps to, and which prior plan (01, 02, or 03) should be re-opened to fix.
    - The `open` command for the browser checkpoint is constructed (not yet executed) and recorded.
  </acceptance_criteria>
  <done>All 15 automated checks executed; results captured for the SUMMARY; ready for Task 2 human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human browser-verify Phase 1 Home page</name>
  <what-built>
    Plans 01–03 produced the complete Phase 1 deliverable:
    - `CNAME` with `ivolinengong.com`
    - `assets/profile_pic.png` and `assets/Ivoline_Ngong_CV_August_2025.pdf`
    - `style.css` implementing the UI-SPEC token/typography/layout/news/contact contract
    - `index.html` with verbatim bio, scrollable news, contact strip, profile photo, full 5-item nav, and the page metadata (title `Ivoline Ngong`, viewport meta, UI-SPEC description meta)

    Task 1 of this plan ran automated checks confirming files exist, CNAME content is correct, no Wayback artifacts, no scripts, no CDN dependencies, no Jekyll/Node residue.

    What remains is the visual + interaction proof — only a human can confirm "looks right" against the Carlini-style aesthetic captured in UI-SPEC.
  </what-built>
  <how-to-verify>
    Open the page in your default browser. From a terminal at the repo root run:

        open /Users/ivolinengong/Documents/My_Website/index.html

    (Or double-click `index.html` in Finder. Or drag it onto a browser window.)

    Then check the following items, in order. Each item maps to a Phase 1 success criterion (ROADMAP §Phase 1 / Success Criteria). Note any failure with as much detail as possible (which item, what you saw vs what was expected, browser used).

    **(SC1) Content verbatim from wayback**
    1. The page heading shows `Ivoline Ngong` (large, weight 700).
    2. Below the heading: `PhD Candidate · University of Vermont` in muted gray.
    3. The bio section reads naturally — three or four paragraphs covering: 4th-year UVM PhD, OpenMined research scientist, advised by Joe Near, intersection of privacy/security/safety/AI; the three research questions paragraph; the IBM Research summer with named mentors (Karthikeyan Natesan Ramamurthy, Hao Wang, Amit Dhurandhar) with their names linked; the "Fun fact / three continents" paragraph; the "Interested in collaborating? — email me" closing line.
    4. Bio text reads exactly like the wayback original (`reference/wayback/Home - Ivoline Ngong.html`) — no paraphrase, no truncation.

    **(SC2) Scrollable news container**
    5. There is a clearly delineated bordered box under the `What's New?` heading.
    6. The box has a fixed visible height (~380px on a desktop window); entries below the visible window are reachable by scrolling INSIDE the box (use mouse wheel over the box, or two-finger scroll on trackpad).
    7. Scrolling inside the box does NOT scroll the rest of the page.
    8. The box has its own subtle scrollbar on the right edge.
    9. The most recent entries are at the top (reverse chronological).
    10. Entry count is at least 15 (Plan 03 SUMMARY records the exact number — compare).

    **(SC3) CV link works**
    11. Find the contact strip directly under the role line.
    12. Click `CV`. The browser should either:
        - open the PDF inline in a new tab / same tab (most browsers do this for local PDFs), OR
        - prompt to download it.
    13. Either outcome is success; both prove the link resolves to `assets/Ivoline_Ngong_CV_August_2025.pdf`. A 404 or "file not found" is failure.

    **(SC3 + extras) Other contact links**
    14. Hover over `ivolinengong@gmail.com` — status bar should show `mailto:ivolinengong@gmail.com`. (Optional: click to confirm mail client opens; not required to PASS.)
    15. Hover over `Twitter` — status bar should show `https://twitter.com/IvolineNgong` (no `web.archive.org` prefix).
    16. Hover over `Scholar` — status bar should show a `scholar.google.com` URL with `user=DzEo62wAAAAJ`.
    17. Hover over `GitHub` — status bar should show `https://github.com/ivyclare`.

    **(SC4) Teal accent + Carlini-style styling**
    18. Links are teal (`#0F766E` ish) and underlined.
    19. Body text is dark gray on a white background.
    20. Page is single-column, centered, with comfortable side margins.
    21. There is no decorative chrome — no boxes, no shadows, no gradients, no animations, no footer.
    22. The profile photo is circular (border-radius: 50%), ~120px square on desktop, positioned to the right of the H1 group.

    **(SC5) Repo shape — no need to look at this in browser, but a final sanity check**
    23. (Already verified by Task 1.) From a terminal: `ls -la && ls assets/` shows the expected layout.

    **(SC6) Visual feel matches Carlini-minimal**
    24. The page feels content-first. Nothing competes with the bio + news + links for attention.
    25. No external resources fail to load (open browser devtools → Network tab → reload → no red 4xx/5xx rows; only `style.css` and `assets/profile_pic.png` should appear, plus the HTML itself).
    26. Open browser devtools → Console tab. Zero red errors. (Yellow warnings about missing favicon are acceptable for Phase 1; favicon is Phase 3 work.)

    **(Bonus) Top nav**
    27. The top of the page shows: `Main · Papers · Talks · Code · Writing`.
    28. `Main` is teal (active state).
    29. `Papers`, `Talks`, `Code`, `Writing` are gray and NOT clickable (they are `<span>` placeholders for Phase 2). Hovering them should not show a link-cursor or status-bar URL — that confirms they're spans, not broken links to 404 destinations.

    **(Bonus) Skip link a11y**
    30. Press Tab once when the page is freshly focused. The first focusable element should be a "Skip to main content" link that pops into view (top-left, teal background).
    31. Press Tab again. Focus should land in the main content (or the next interactive element).
    32. Tab around the page. Focus rings should be visible (2px teal outline) on each link and on the news container itself (the news container is focusable per UI-SPEC §Scrollable News).

    **Resume signal:**
    - If everything looks correct, type `approved`.
    - If anything is wrong, list each issue with the format: `(SC{N}) item {n}: expected X, got Y` so the gap-closure planner can scope a fix plan.
  </how-to-verify>
  <resume-signal>Type "approved" if the Home page renders correctly per all 32 items above, or list issues using the `(SCn) item n: expected ... , got ...` format.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ← local file `index.html` | Reading static files only. No network at runtime (except outbound clicks). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Information disclosure | Browser may log opened-file URL to history | accept | Local file paths in browser history are a developer-machine concern, not a deployed-site concern. |
| T-01-04-02 | Tampering | A reviewer claims PASS without actually opening the page | mitigate | The 32-item checklist is granular enough that a careless approval is obvious — the orchestrator can re-ask if the SUMMARY shows no issues but the checklist wasn't engaged. |
| T-01-04-03 | Denial of service | Browser fails to render due to file:// CSP limitation | accept | Modern browsers render plain HTML+CSS without issue via `file://`. No fetch / no module imports / no service worker in this page. If a browser objects (very unlikely for plain static HTML), Task 2 surfaces it as a failure for gap closure. |
| T-01-04-04 | Supply chain | Third-party packages | accept | No installs in this plan. Read-only verification. |

Block-on severity: none. This plan reads and inspects; it does not modify.
</threat_model>

<verification>
- All 15 automated checks in Task 1 PASS (or are surfaced as failures for follow-up).
- Human approves the Task 2 checkpoint by typing `approved` after walking the 32-item checklist in a browser.
- SUMMARY records: automated results, browser used (e.g., "Safari 17, macOS 14"), each item's status, any defects with full repro detail.
- If human flags any defect: SUMMARY converts to a gap report — list each `(SCn) item n` with expected/actual, so `/gsd-plan-phase 1 --gaps` (or a follow-up plan) can scope the fix.
</verification>

<success_criteria>
- [ ] All 15 automated checks in Task 1 executed and recorded.
- [ ] Task 2 checkpoint completed (human-verified).
- [ ] If approved: Phase 1 is complete and ready to close. ROADMAP can mark Phase 1 done.
- [ ] If defects: every defect is logged in the SUMMARY in the gap-closure-friendly `(SCn) item n: expected ..., got ...` format.
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-home-mvp/01-04-SUMMARY.md` documenting:
- Task 1 automated check results (15 items: PASS/FAIL + actual values)
- Browser used for Task 2 checkpoint
- Task 2 checklist results (32 items: each PASS/FAIL with notes)
- If approved: a one-line "Phase 1 PASS — ready to close" header
- If defects: each defect formatted `(SCn) item n: expected X, got Y`, sortable by Phase 1 success criterion for downstream gap-closure planning
- Profile photo file size (carry-forward note for Phase 3 QUAL-06 image optimization)
- LinkedIn decision carry-forward (from Plan 03 SUMMARY) — either "owner-action: provide LinkedIn URL" or "extracted from body grep"
- Any other carry-forward items for Phase 2 or Phase 3
</output>
