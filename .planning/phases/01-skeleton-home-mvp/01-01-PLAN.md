---
phase: 01-skeleton-home-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CNAME
  - assets/profile_pic.png
  - assets/Ivoline_Ngong_CV_August_2025.pdf
  - .gitignore
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-06
  - PAGE-07
  - PAGE-08
tags:
  - static-site
  - github-pages
  - repo-scaffolding

must_haves:
  truths:
    - "`CNAME` at repo root contains exactly the string `ivolinengong.com` on a single line with no trailing whitespace or comment."
    - "`assets/profile_pic.png` exists and `profile_pic.png` no longer exists at repo root."
    - "`assets/Ivoline_Ngong_CV_August_2025.pdf` exists and `Ivoline_Ngong_CV_August_2025.pdf` no longer exists at repo root."
    - "`.gitignore` prevents `.DS_Store` from being tracked."
  artifacts:
    - path: "CNAME"
      provides: "GitHub Pages custom domain binding to ivolinengong.com"
      contains: "ivolinengong.com"
    - path: "assets/profile_pic.png"
      provides: "Owner headshot at the path that index.html will reference"
    - path: "assets/Ivoline_Ngong_CV_August_2025.pdf"
      provides: "CV PDF at the path the Home page CV link will target"
    - path: ".gitignore"
      provides: "Keeps macOS metadata files out of the repo"
      contains: ".DS_Store"
  key_links:
    - from: "index.html (Plan 03)"
      to: "assets/profile_pic.png"
      via: "<img src=\"assets/profile_pic.png\">"
      pattern: "src=\"assets/profile_pic\\.png\""
    - from: "index.html (Plan 03)"
      to: "assets/Ivoline_Ngong_CV_August_2025.pdf"
      via: "<a href=\"assets/Ivoline_Ngong_CV_August_2025.pdf\">CV</a>"
      pattern: "href=\"assets/Ivoline_Ngong_CV_August_2025\\.pdf\""
---

<objective>
Lay down the deployable repo skeleton: create the `CNAME` file for GitHub Pages custom-domain binding, create the `assets/` directory, move the two owner-provided binary assets into it using `git mv` (so history is preserved), and add a minimal `.gitignore` to keep `.DS_Store` out of the repo.

Purpose: This is the foundation every subsequent plan in Phase 1 builds on. `index.html` (Plan 03) references both `assets/profile_pic.png` and `assets/Ivoline_Ngong_CV_August_2025.pdf` — those paths must resolve at build time. The `CNAME` makes the repo deployable to `ivolinengong.com` as-is.

Output: Updated repo layout with `CNAME`, `.gitignore`, and `assets/` directory populated with the two relocated binaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/HANDOFF.md
@PROJECT-BRIEF.md
@.planning/phases/01-skeleton-home-mvp/01-UI-SPEC.md

<rationale>
Phase 1 itself is the Walking Skeleton for the entire site. Plan 01 is the scaffolding step — repo layout + binary asset relocation + CNAME. There is no DB, no routing, no framework; the "skeleton" manifests as: deployable repo structure (CNAME) + asset directory (so HTML can link relatively) + ignored OS junk. Do NOT emit a separate SKELETON.md — the four Phase 1 PLAN.md files collectively ARE the skeleton documentation.
</rationale>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CNAME, .gitignore, and assets/ directory at repo root</name>
  <files>CNAME, .gitignore, assets/</files>
  <read_first>
    - .planning/REQUIREMENTS.md (verify INFRA-01 text: CNAME contains EXACTLY `ivolinengong.com`)
    - .planning/HANDOFF.md (critical reminders section — CNAME content, .DS_Store rule)
  </read_first>
  <action>
    Create three artifacts at the repo root.

    1. `CNAME` — write a single line containing the canonical domain string `ivolinengong.com`. No comment line, no trailing whitespace, no second line. Use the Write tool; do NOT use shell heredoc. INFRA-01 requires EXACTLY this content. A trailing newline byte is acceptable (most editors add one) but no extra characters.

    2. `.gitignore` — add the following entries, one per line: `.DS_Store`, `*.swp`, `node_modules/` (defensive — repo should never need node, but if a contributor accidentally `npm install`s, ignore it), `.idea/`, `.vscode/`. Per HANDOFF reminder: macOS `.DS_Store` files are already loose in the repo (one is shown at the top of the working tree) — `.gitignore` prevents future ones from being tracked. Do NOT delete the existing `.DS_Store` file in this task; that is out of scope. The git index will simply stop tracking new ones.

    3. `assets/` — create the empty directory. On Unix you cannot commit an empty directory directly, but Task 2 will move files into it in the same plan, so the directory will be non-empty by commit time. If using `mkdir`, use `mkdir -p assets`.

    Do not create `index.html`, `style.css`, or `README.md` here — those belong to later plans or phases.
  </action>
  <verify>
    <automated>test -f CNAME &amp;&amp; test "$(cat CNAME | tr -d '\n')" = "ivolinengong.com" &amp;&amp; test -d assets &amp;&amp; test -f .gitignore &amp;&amp; grep -q '^\.DS_Store$' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `CNAME` exists at repo root.
    - `CNAME` content, with newlines stripped, equals exactly `ivolinengong.com` (no trailing comment, no extra characters).
    - `.gitignore` exists at repo root and includes a line `.DS_Store` (regex `^\.DS_Store$`).
    - `assets/` directory exists at repo root.
  </acceptance_criteria>
  <done>CNAME, .gitignore, and assets/ directory all present; CNAME content is byte-for-byte the domain string plus optional terminating newline.</done>
</task>

<task type="auto">
  <name>Task 2: Move profile_pic.png and CV PDF into assets/ using git mv</name>
  <files>profile_pic.png → assets/profile_pic.png, Ivoline_Ngong_CV_August_2025.pdf → assets/Ivoline_Ngong_CV_August_2025.pdf</files>
  <read_first>
    - .planning/HANDOFF.md ("Move assets into `assets/`" — files must be moved, not copied)
    - .planning/REQUIREMENTS.md (PAGE-07 says `profile_pic.png` moved into assets/; PAGE-08 says CV PDF moved into assets/)
  </read_first>
  <action>
    Relocate the two owner-provided binary files from repo root into `assets/` using `git mv` so git tracks the rename rather than treating it as delete+add. Preserving move history matters because both files are pre-existing committed assets the owner placed in the repo before planning began.

    Concrete commands (run from repo root):
    - `git mv profile_pic.png assets/profile_pic.png`
    - `git mv Ivoline_Ngong_CV_August_2025.pdf assets/Ivoline_Ngong_CV_August_2025.pdf`

    Path-and-filename invariants (downstream tasks depend on exact strings):
    - Final image path must be EXACTLY `assets/profile_pic.png` (lowercase, no underscore changes). Plan 03's `<img src="...">` will hard-code this string.
    - Final CV path must be EXACTLY `assets/Ivoline_Ngong_CV_August_2025.pdf` (preserving capitalization, underscores, and the August_2025 suffix). Plan 03's CV link will hard-code this string. CONT-04 and PAGE-08 are both checked against this exact filename.

    Do NOT rename, rewrite, optimize, resize, or re-encode either file. Plan 04 may flag if the PNG is unusually large (~3MB at present), but compression is Phase 3 work, not Phase 1.

    If `git mv` fails because either file is currently uncommitted/untracked, fall back to filesystem `mv` and `git add`/`git rm` appropriately, but the files in fact were committed in earlier project setup commits (see HANDOFF git log), so `git mv` should succeed.
  </action>
  <verify>
    <automated>test -f assets/profile_pic.png &amp;&amp; test -f assets/Ivoline_Ngong_CV_August_2025.pdf &amp;&amp; test ! -f profile_pic.png &amp;&amp; test ! -f Ivoline_Ngong_CV_August_2025.pdf</automated>
  </verify>
  <acceptance_criteria>
    - `assets/profile_pic.png` exists and is a valid PNG (first 8 bytes start with `\x89PNG\r\n\x1a\n`).
    - `assets/Ivoline_Ngong_CV_August_2025.pdf` exists and is a valid PDF (first 4 bytes are `%PDF`).
    - `profile_pic.png` no longer exists at repo root.
    - `Ivoline_Ngong_CV_August_2025.pdf` no longer exists at repo root.
    - `git status` shows both files as renames (with `R` status) rather than delete+add pairs.
  </acceptance_criteria>
  <done>Both binary assets are now under `assets/`, removed from repo root, with rename tracked by git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | This plan only manipulates static files locally. No network, no input parsing, no execution context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information disclosure | `assets/Ivoline_Ngong_CV_August_2025.pdf` | accept | The owner intentionally publishes the CV on the public site. Hosting the file under `assets/` is the explicit design intent (PROJECT-BRIEF §6, CONT-04, PAGE-08). No PII beyond what the owner has already chosen to make public. |
| T-01-02 | Information disclosure | `.DS_Store` accidentally tracked | mitigate | `.gitignore` includes `.DS_Store` so macOS metadata never enters the commit graph. The existing `.DS_Store` at repo root will be cleaned up in Plan 04 (verification plan) or later — out of scope for this plan. |
| T-01-03 | Tampering | None | accept | No third-party packages installed, no scripts run. Package-legitimacy gate not applicable to this plan. |

Block-on severity: none. All threats are accepted or mitigated by construction.
</threat_model>

<verification>
After both tasks complete, verify the repo layout matches the Phase 1 target shape (relevant subset):

```
/Users/ivolinengong/Documents/My_Website/
├── CNAME                     # contains exactly: ivolinengong.com
├── .gitignore                # ignores .DS_Store
├── assets/
│   ├── profile_pic.png       # MOVED from root
│   └── Ivoline_Ngong_CV_August_2025.pdf  # MOVED from root
└── ... (other pre-existing files: .planning/, reference/, PROJECT-BRIEF.md, CLAUDE.md, .git/)
```

Run a final sanity command from repo root:
- `ls assets/` should list both files
- `ls profile_pic.png Ivoline_Ngong_CV_August_2025.pdf 2>&1 | grep -c "No such"` should equal `2`
- `cat CNAME` should print `ivolinengong.com` and nothing else
</verification>

<success_criteria>
- [ ] CNAME file exists, content is exactly `ivolinengong.com` (one line).
- [ ] `.gitignore` exists and ignores `.DS_Store`.
- [ ] `assets/` directory exists.
- [ ] `assets/profile_pic.png` exists, root-level `profile_pic.png` is gone.
- [ ] `assets/Ivoline_Ngong_CV_August_2025.pdf` exists, root-level CV is gone.
- [ ] Both moves tracked as renames by git.
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-home-mvp/01-01-SUMMARY.md` when done documenting:
- Final repo layout snapshot (`ls -la` and `ls assets/`)
- `CNAME` exact content
- `.gitignore` contents
- Confirmation both moves were git-tracked renames (`git status` excerpt)
- Any deviations (none expected)
</output>
