---
phase: 02-remaining-content-pages
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - talks.html
autonomous: true
requirements:
  - CONT-06
  - PAGE-03
  - PAGE-06
tags:
  - static-site
  - content-extraction
  - talks-page
must_haves:
  truths:
    - "A visitor opening talks.html sees a page-title `<h2>Talks</h2>` and below it 3 talk entries extracted verbatim from `reference/wayback/Talks - Ivoline Ngong.html`, in source order (Microsoft Research → University of Buea → CS Research Day UVM)."
    - "Each talk entry renders: talk title, venue, year, and at least one resource link (Video / Slides / Feature) where the wayback source provides one."
    - "The shared site-header + site-nav appears at the top identical to other Phase-2 pages, with the `Talks` nav item carrying `aria-current=\"page\"`."
    - "All recovered URLs are canonical — no `web.archive.org` prefix anywhere."
  artifacts:
    - path: "talks.html"
      provides: "Talks page — 3 invited talks with venue, year, and resource links"
      min_lines: 50
      contains: "<h2>Talks</h2>"
  key_links:
    - from: "talks.html nav"
      to: "index.html / publications.html / writing.html / code.html"
      via: '<a href> elements in <nav class="site-nav">'
      pattern: 'site-nav.*href="(index|publications|writing|code)\.html"'
---

# Phase 2 Plan 03 — talks.html (Talks listing)

## Phase Goal

**As a** visitor to ivolinengong.com, **I want to** open `talks.html` and see a chronological list of Ivoline's invited talks with venues and links to recordings or slides where available, **so that** I can watch or read her presentations directly.

<objective>
Create `talks.html` at repo root — a single static HTML file rendering the 3 talks listed on the wayback Talks page, extracted verbatim and rendered on the shared site shell with Talks marked as the current nav item.

Purpose: Phase 2 deliverable. The wayback Talks page is structurally the simplest of the four Phase-2 pages — 3 `<p>` paragraphs, each with title + venue + year + 1-2 inline links. This plan re-expresses them using the `.talk-entry` class established in Plan 02-01.

Output: 1 new file `talks.html` at the repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-home-mvp/01-UI-SPEC.md
@index.html
@style.css
@reference/wayback/Talks - Ivoline Ngong.html

<interfaces>
<!-- The site shell is identical across all Phase-2 pages; only the page <title>, <meta description>, and the aria-current target differ. -->

The 3 talks (extracted from wayback, lines 682-693, with Wayback prefixes already stripped):

1. **Compiler Optimizations using Reinforcement Learning** — Microsoft Research 2022 — Video: `https://www.youtube.com/watch?v=jnkxDdZF_gc&t=781s` — Feature: `https://www.microsoft.com/en-us/research/academic-program/rl-open-source-fest/alumni/`
2. **Fairness and Privacy in AI** — University of Buea 2022 — Video: `https://youtu.be/9zFhJ5vjBtw`
3. **Continual Audit of Individual and Group Fairness in Deployed Classifiers via Prediction Sensitivity** — CS Research Day, University of Vermont, 2021 — Slides: `https://drive.google.com/file/d/1TLQXATVglRZYbVRz73XNA2Kn0xLIK-4x/view?usp=sharing`

Per-entry markup contract (uses `.talk-entry` from Plan 02-01):
```html
<article class="talk-entry">
  <p class="talk-body">TITLE, VENUE YEAR. <a href="URL1">LinkLabel1</a> | <a href="URL2">LinkLabel2</a></p>
</article>
```

The wayback source renders each talk as a single flowing `<p>` paragraph with title + venue + year as plain prose and resource links inline at the end, separated by ` | ` (space-pipe-space) in the original. We mirror that exactly. The pipe separator in this paragraph is rendered as a literal ` | ` (with surrounding spaces) within the prose — NOT a `<span class="pub-sep">` element — because here it's part of the running prose, not a structured link row.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write talks.html (shell + nav + 3 talk entries) in a single pass</name>
  <files>talks.html</files>
  <read_first>
    - /Users/ivolinengong/Documents/My_Website/index.html (verbatim source of the site shell pattern; lines 1-22 are the template)
    - /Users/ivolinengong/Documents/My_Website/reference/wayback/Talks - Ivoline Ngong.html (PRIMARY SOURCE — read lines 670-700 to extract the 3 talks)
    - /Users/ivolinengong/Documents/My_Website/.planning/phases/01-skeleton-home-mvp/01-UI-SPEC.md (sections "Copywriting Contract", "Site header") — confirms title format and nav rules
    - /Users/ivolinengong/Documents/My_Website/.planning/phases/02-remaining-content-pages/02-01-PLAN.md (read the `<interfaces>` section to confirm `.talk-entry` class contract)
  </read_first>
  <action>
    Create `/Users/ivolinengong/Documents/My_Website/talks.html` containing the full page (shell + 3 entries) in one pass. Because there are only 3 entries the work fits naturally in a single task.

    **1. Site shell** — identical structure to publications.html scaffolding (Plan 02-02 Task 1), but with these deltas:
       - `<title>Talks — Ivoline Ngong</title>` (em-dash, matches UI-SPEC §Copywriting Contract)
       - `<meta name="description" content="Invited talks by Ivoline Ngong on privacy, security, fairness, and AI.">` (end with a period for parity with publications.html)
       - In the nav, swap which item carries `aria-current="page"`: `Talks` is a `<span>`, all others are `<a>`. Final nav order:
         - `<a href="index.html">Main</a>`
         - `<a href="publications.html">Papers</a>`
         - `<span aria-current="page">Talks</span>`
         - `<a href="code.html">Code</a>`
         - `<a href="writing.html">Writing</a>`
       - `<a class="site-title" href="index.html">Ivoline Ngong</a>` — NO `aria-current` (not Home).

    **2. Page heading** — inside `<main id="main">`, the first child is `<h2>Talks</h2>`.

    **3. Talk entries** — emit 3 `<article class="talk-entry">` blocks in wayback source order (Microsoft Research → Buea → UVM CS Research Day). For each talk:

       ```html
       <article class="talk-entry">
         <p class="talk-body">TITLE, VENUE YEAR. <a href="URL1">LABEL1</a> | <a href="URL2">LABEL2</a></p>
       </article>
       ```

       Concrete contents (extract from the wayback file to verify each byte, but here is the expected post-strip form — re-confirm against source before writing):

       **Talk 1** (wayback line 684):
       - Body text begins: `Compiler Optimizations using Reinforcement Learning, Microsoft Research 2022.`
       - Then a space, then `<a href="https://www.youtube.com/watch?v=jnkxDdZF_gc&amp;t=781s">Video</a>` (preserve `&amp;` for the URL ampersand)
       - Then ` |&nbsp;` (a space, a pipe, then `&nbsp;` — the wayback source uses `&nbsp;` between the pipe and the next link; preserve it)
       - Then `<a href="https://www.microsoft.com/en-us/research/academic-program/rl-open-source-fest/alumni/">Feature</a>`

       **Talk 2** (wayback line 688):
       - Body text begins: `Fairness and Privacy in AI, University of Buea 2022.`
       - Then `&nbsp;` (the wayback source uses `&nbsp;` as the space between the period and the link; preserve it)
       - Then `<a href="https://youtu.be/9zFhJ5vjBtw">Video</a>`

       **Talk 3** (wayback line 692):
       - Body text begins: `Continual Audit of Individual and Group Fairness in Deployed Classifiers via Prediction Sensitivity, CS Research Day, University of Vermont, 2021.`
       - Then a space, then `<a href="https://drive.google.com/file/d/1TLQXATVglRZYbVRz73XNA2Kn0xLIK-4x/view?usp=sharing">Slides</a>`

    **4. Extraction invariants** (Phase 1 protocol, see 01-03-SUMMARY §decisions):
       - Strip `https://web.archive.org/web/{14-digit-timestamp}/` prefix from EVERY `href`.
       - DROP `style="background-color: #ffffff; color: #009688;"` and `style="color: #009688;"` inline styles on the wayback `<a>` elements — the global `a { color: var(--accent); }` rule in style.css handles link coloring; no inline styles allowed (DES-01 invariant).
       - DROP `target="_blank"` and `rel="noopener"` from every link (Phase 1 invariant: zero `target="_blank"`).
       - PRESERVE `&amp;` HTML entities verbatim (do not convert to `&`).
       - PRESERVE `&nbsp;` where the wayback source uses it as a separator (talk 1 + talk 2). Cosmetic detail but matches the original spacing.
       - PRESERVE source typos verbatim (none observed in the talks block, but if any are spotted during extraction, document them in the SUMMARY).
       - Do NOT reorder, paraphrase, or compress any title or venue string.

    **5. Page closing**: after the third `<article>`, close `</main></body></html>` with a single trailing newline. NO `<footer>`, NO `<script>`, NO inline `<style>`.

    Indentation: 2 spaces for top-level structure, 4 spaces inside `<main>`, matching the convention from index.html and publications.html.
  </action>
  <verify>
    <automated>
      cd /Users/ivolinengong/Documents/My_Website &amp;&amp;
      test -f talks.html &amp;&amp;
      grep -iq '<!doctype html>' talks.html &amp;&amp;
      grep -q '<title>Talks — Ivoline Ngong</title>' talks.html &amp;&amp;
      grep -q 'href="style.css"' talks.html &amp;&amp;
      grep -q 'class="skip-link"' talks.html &amp;&amp;
      grep -q '<a class="site-title" href="index.html">Ivoline Ngong</a>' talks.html &amp;&amp;
      grep -q '<a href="index.html">Main</a>' talks.html &amp;&amp;
      grep -q '<a href="publications.html">Papers</a>' talks.html &amp;&amp;
      grep -q '<span aria-current="page">Talks</span>' talks.html &amp;&amp;
      grep -q '<a href="code.html">Code</a>' talks.html &amp;&amp;
      grep -q '<a href="writing.html">Writing</a>' talks.html &amp;&amp;
      grep -q '<h2>Talks</h2>' talks.html &amp;&amp;
      [ "$(grep -c '<article class="talk-entry">' talks.html)" = "3" ] &amp;&amp;
      [ "$(grep -c '<p class="talk-body">' talks.html)" = "3" ] &amp;&amp;
      grep -q 'Compiler Optimizations using Reinforcement Learning' talks.html &amp;&amp;
      grep -q 'Fairness and Privacy in AI' talks.html &amp;&amp;
      grep -q 'Continual Audit of Individual and Group Fairness' talks.html &amp;&amp;
      grep -q 'youtube.com/watch?v=jnkxDdZF_gc' talks.html &amp;&amp;
      grep -q 'youtu.be/9zFhJ5vjBtw' talks.html &amp;&amp;
      grep -q 'drive.google.com/file/d/1TLQXATVglRZYbVRz73XNA2Kn0xLIK-4x' talks.html &amp;&amp;
      grep -q 'microsoft.com/en-us/research/academic-program/rl-open-source-fest' talks.html &amp;&amp;
      # Invariants
      [ "$(grep -c 'web.archive.org' talks.html)" = "0" ] &amp;&amp;
      [ "$(grep -c 'target="_blank"' talks.html)" = "0" ] &amp;&amp;
      [ "$(grep -c 'rel="noopener"' talks.html)" = "0" ] &amp;&amp;
      [ "$(grep -c '<script' talks.html)" = "0" ] &amp;&amp;
      [ "$(grep -c 'style="background-color' talks.html)" = "0" ] &amp;&amp;
      [ "$(grep -c 'style="color' talks.html)" = "0" ] &amp;&amp;
      ! grep -qE '<a[^>]*aria-current' talks.html &amp;&amp;
      [ "$(wc -l &lt; talks.html)" -ge "30" ] &amp;&amp;
      echo "PASS: talks.html written with 3 verbatim entries"
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] `talks.html` exists at repo root
    - [ ] `<title>` is exactly `Talks — Ivoline Ngong`
    - [ ] Skip link is the first body child
    - [ ] `<header class="site-header">` contains site-title link to `index.html` WITHOUT `aria-current`
    - [ ] Nav has 5 children in order Main / Papers / Talks / Code / Writing
    - [ ] `Talks` is the ONLY nav item carrying `aria-current="page"`, and it is a `<span>`
    - [ ] Exactly 3 `<article class="talk-entry">` elements, each containing exactly one `<p class="talk-body">`
    - [ ] Talk 1 contains both the YouTube Video link AND the Microsoft Feature link
    - [ ] Talk 2 contains the youtu.be Video link
    - [ ] Talk 3 contains the Google Drive Slides link
    - [ ] Zero `web.archive.org`, zero `target="_blank"`, zero `rel="noopener"`, zero `<script>`, zero `style="…"` inline styles
    - [ ] No anchor carries `aria-current`
    - [ ] File ends with `</main></body></html>` and a single trailing newline
  </acceptance_criteria>
  <done>talks.html renders the 3 wayback talks verbatim using `.talk-entry` from Plan 02-01; zero Wayback prefixes, zero target=_blank, zero inline styles, zero scripts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← talks.html | Static file; outbound links cross to YouTube, Google Drive, and Microsoft Research. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Spoofing | External Video/Slides/Feature links | accept | Owner-curated URLs to YouTube, Google Drive, microsoft.com — canonical destinations the owner chose. |
| T-02-03-02 | Tampering | Wayback-prefix injection | mitigate | Strip protocol on every URL; automated grep verifies zero `web.archive.org` substrings post-write. |
| T-02-03-03 | Spoofing (tabnabbing) | External links | mitigate | No `target="_blank"` anywhere → no tabnabbing surface (Phase 1 invariant preserved). |
| T-02-03-04 | Information Disclosure | Talk titles | accept | All talks are publicly delivered; no PII beyond what owner published. |
| T-02-03-05 | Repudiation / Integrity | Verbatim content claim | mitigate | CONT-09: no fabrication. Manual cross-check of all 3 entries against wayback source during extraction. |
| T-02-03-06 | Denial of Service | Page weight | accept | ~30-50 line static HTML with no images. |
| T-02-03-SC | Tampering | package installs | n/a | Zero packages installed in this plan. |
</threat_model>

<verification>
End-of-plan verification:

```bash
cd /Users/ivolinengong/Documents/My_Website
test -f talks.html || echo FAIL: file missing
[ "$(grep -c '<article class="talk-entry">' talks.html)" = "3" ] || echo FAIL: wrong article count
grep -q '<title>Talks — Ivoline Ngong</title>' talks.html || echo FAIL: title wrong
grep -q '<span aria-current="page">Talks</span>' talks.html || echo FAIL: Talks not marked current
! grep -q 'web.archive.org' talks.html || echo FAIL: wayback prefix leaked
! grep -q 'target="_blank"' talks.html || echo FAIL: target=_blank introduced
! grep -q '<script' talks.html || echo FAIL: script tag introduced
! grep -qE 'style="(background-color|color)' talks.html || echo FAIL: inline style introduced
# Cross-page nav consistency
for label_file in Main:index Papers:publications Code:code Writing:writing; do
  label="${label_file%:*}"; f="${label_file#*:}"
  grep -q "<a href=\"${f}.html\">${label}</a>" talks.html || echo "FAIL: nav link to ${f}.html missing"
done
echo "VERIFICATION COMPLETE"
```
</verification>

<success_criteria>
1. talks.html exists at repo root and opens cleanly as a local file
2. The shared site-header + nav renders identical to other Phase-2 pages, with Talks marked as the current page
3. Below the header, an `<h2>Talks</h2>` heading
4. 3 talk entries in wayback source order: Microsoft Research (2022) → University of Buea (2022) → CS Research Day UVM (2021)
5. Each talk's title, venue, and year is verbatim from the wayback source
6. Each talk's resource link(s) use canonical URLs (no `web.archive.org` prefix)
7. The page works with JavaScript disabled (zero scripts)
8. Clicking Main / Papers / Code / Writing nav items navigates to those pages (each is a sibling Wave-2 plan)
</success_criteria>

<output>
Create `.planning/phases/02-remaining-content-pages/02-03-SUMMARY.md` documenting:
- The 3 talks extracted with their canonical URLs in a table (title | venue | year | links)
- Any source-text typos preserved verbatim
- Confirmation that no Wayback prefixes, no `target="_blank"`, no inline styles, no `<script>` tags leaked into the final HTML
- Threat-model dispositions honored
</output>
