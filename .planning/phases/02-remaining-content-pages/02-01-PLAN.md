---
phase: 02-remaining-content-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - style.css
autonomous: true
requirements:
  - PAGE-06
tags:
  - static-site
  - nav
  - css
must_haves:
  truths:
    - "A visitor on Home (index.html) can click any of the 4 nav items (Papers/Talks/Code/Writing) and the browser attempts to navigate to publications.html / talks.html / code.html / writing.html (even if those files don't exist yet — the anchor is real)."
    - "All 4 previously-placeholder nav items render in the teal `--accent` color via the existing `.site-nav a` rule, not the muted-gray placeholder rule."
    - "On `index.html`, the 'Main' nav item still carries `aria-current=\"page\"` and no other nav item does."
    - "style.css contains class rules for `.pub-entry`, `.talk-entry`, `.writing-entry`, and `.writing-year-group` that the Wave-2 content pages can render against without inventing new conventions."
  artifacts:
    - path: "index.html"
      provides: "Activated nav: Papers/Talks/Code/Writing rendered as <a href> elements, not <span data-todo=\"phase-2\"> placeholders"
      contains: 'href="publications.html"'
    - path: "style.css"
      provides: "Content-entry layout classes used by all Wave-2 Phase-2 pages"
      contains: ".pub-entry"
  key_links:
    - from: "index.html nav"
      to: "publications.html / talks.html / code.html / writing.html"
      via: '<a href> elements in <nav class="site-nav">'
      pattern: 'site-nav.*href="(publications|talks|code|writing)\.html"'
    - from: "style.css"
      to: "Wave-2 plans (02-02, 02-03, 02-04)"
      via: ".pub-entry / .talk-entry / .writing-entry class selectors"
      pattern: "\\.(pub|talk|writing)-entry"
---

# Phase 2 Plan 01 — Foundation: Activate Nav + Content-Entry CSS

## Phase Goal

**As a** visitor to ivolinengong.com, **I want to** navigate from the Home page to Publications, Talks, Writing, and Code via a shared nav that appears identically on every page, **so that** I can browse Ivoline's full body of work without using the browser back button or guessing URLs.

<objective>
Activate the four Phase-1 nav placeholders in `index.html` so they become real `<a>` anchors pointing at the Phase-2 HTML files, and add the minimal CSS classes that Wave-2 content pages (`publications.html`, `talks.html`, `writing.html`) will render against. This is the Wave-1 foundation that all four content-page plans depend on.

Purpose: Phase 1 deliberately stubbed Papers/Talks/Code/Writing as `<span data-todo="phase-2">` placeholders to avoid 404s before the files existed. This plan flips them to real `<a href>` links and prepares `style.css` for the new content patterns. Activating early (Wave 1) means downstream plans (02-02..02-05) can deliver visually-complete pages without later patching the home page.

Output: 1 modified file (`index.html`, nav block lines 15-21), 1 modified file (`style.css`, new content-entry rules appended), 0 new files. Code page page is created in Plan 02-05 — this plan only declares its href in the nav.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-home-mvp/01-UI-SPEC.md
@.planning/phases/01-skeleton-home-mvp/01-03-SUMMARY.md
@index.html
@style.css

<interfaces>
<!-- Current nav block in index.html (lines 15-21) — Phase 1 left these as placeholder spans. -->
<!-- The contract: replace 4 spans with <a> anchors. Do NOT touch the Main item or the .site-header structure. -->

From index.html (lines 13-22, verbatim):
```html
  <header class="site-header">
    <a class="site-title" href="index.html" aria-current="page">Ivoline Ngong</a>
    <nav class="site-nav" aria-label="Site">
      <span aria-current="page">Main</span>
      <span data-todo="phase-2">Papers</span>
      <span data-todo="phase-2">Talks</span>
      <span data-todo="phase-2">Code</span>
      <span data-todo="phase-2">Writing</span>
    </nav>
  </header>
```

From style.css (already present — verify before adding new rules):
- `.site-nav a` rule exists at lines 143-147: `color: var(--accent); text-decoration: none;` — so once the 4 spans become anchors, they auto-render in teal. NO new CSS needed for the nav itself.
- `.site-nav [data-todo="phase-2"]` rule at lines 160-163 — becomes dead code after this plan but is harmless; leave it (one or more future spans might reuse it). Do NOT delete it.
- `section h2` rule at lines 308-313 already styles all `<h2>` in `<main>` with small-caps 22px teal-strong — content pages can render their page heading using the existing `<h2>` style as-is.

UI-SPEC nav label → file mapping (locked):
- Main → index.html
- Papers → publications.html
- Talks → talks.html
- Code → code.html
- Writing → writing.html
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Activate the 4 nav placeholders in index.html</name>
  <files>index.html</files>
  <read_first>
    - /Users/ivolinengong/Documents/My_Website/index.html (the full file, but ONLY the nav block at lines 13-22 will be modified)
    - /Users/ivolinengong/Documents/My_Website/.planning/phases/01-skeleton-home-mvp/01-UI-SPEC.md (sections "Site header — top bar" and "Nav label → file mapping" — confirms the href targets)
  </read_first>
  <action>
    Edit `index.html` lines 17-20. Replace each `<span data-todo="phase-2">LABEL</span>` placeholder with the corresponding `<a href="FILE.html">LABEL</a>` anchor. Mapping (exact, per UI-SPEC):

    - Line 17: `<span data-todo="phase-2">Papers</span>` → `<a href="publications.html">Papers</a>`
    - Line 18: `<span data-todo="phase-2">Talks</span>` → `<a href="talks.html">Talks</a>`
    - Line 19: `<span data-todo="phase-2">Code</span>` → `<a href="code.html">Code</a>`
    - Line 20: `<span data-todo="phase-2">Writing</span>` → `<a href="writing.html">Writing</a>`

    Do NOT touch:
    - Line 16 `<span aria-current="page">Main</span>` — Home's own nav item stays as a non-anchor span carrying the current-page marker (UI-SPEC: active page in `--accent`, no underline; non-anchor span enforces this and avoids self-link).
    - The `<a class="site-title" href="index.html" aria-current="page">` on line 14 — this is the duplicate page-identity anchor and is governed by Phase 1; leave it untouched (note: it ALSO has `aria-current="page"` which is technically redundant with the nav span, but Phase 1 shipped it this way — do not regress).
    - Any whitespace, indentation, or surrounding markup. Use single-element replacements only.

    Do NOT use `target="_blank"` on these anchors — they are same-origin same-tab internal links per Phase 1 precedent (zero `target="_blank"` in the existing file).

    Rationale per D-04 (Phase 1 nav placeholders): The placeholders were always intended to become anchors in Phase 2. The CSS `.site-nav a` rule (style.css line 143) already paints anchors in `--accent`, so no CSS work is needed in this task to make the activated nav render correctly. After this edit, viewing index.html should show 5 teal nav items (Main, Papers, Talks, Code, Writing) with Main marked as current page via `aria-current`.
  </action>
  <verify>
    <automated>
      cd /Users/ivolinengong/Documents/My_Website &amp;&amp;
      grep -c 'data-todo="phase-2"' index.html | grep -q '^0$' &amp;&amp;
      grep -q 'href="publications.html">Papers</a>' index.html &amp;&amp;
      grep -q 'href="talks.html">Talks</a>' index.html &amp;&amp;
      grep -q 'href="code.html">Code</a>' index.html &amp;&amp;
      grep -q 'href="writing.html">Writing</a>' index.html &amp;&amp;
      grep -q '<span aria-current="page">Main</span>' index.html &amp;&amp;
      ! grep -q 'target="_blank"' index.html &amp;&amp;
      echo "PASS: nav activated, Main retained, no target=_blank introduced"
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] `grep -c 'data-todo="phase-2"' index.html` returns `0`
    - [ ] The 4 anchor strings `href="publications.html">Papers</a>`, `href="talks.html">Talks</a>`, `href="code.html">Code</a>`, `href="writing.html">Writing</a>` are all present in index.html
    - [ ] `<span aria-current="page">Main</span>` is still present (Main remains a non-anchor span)
    - [ ] No `target="_blank"` anywhere in index.html (Phase 1 invariant preserved)
    - [ ] Total line count of index.html is unchanged (replacements were 1-for-1)
    - [ ] Visual eyeball test (recorded in commit message but not blocking): opening index.html shows 5 teal nav items in order Main, Papers, Talks, Code, Writing.
  </acceptance_criteria>
  <done>Nav placeholders activated as real anchors pointing at the four Phase-2 HTML filenames; Main retains its current-page marker; no other change to index.html.</done>
</task>

<task type="auto">
  <name>Task 2: Append content-entry CSS classes to style.css</name>
  <files>style.css</files>
  <read_first>
    - /Users/ivolinengong/Documents/My_Website/style.css (the full file — must understand existing token names and avoid duplicating selectors)
    - /Users/ivolinengong/Documents/My_Website/.planning/phases/01-skeleton-home-mvp/01-UI-SPEC.md (sections "Typography", "Color", "Spacing Scale" — new rules must use existing tokens only)
  </read_first>
  <action>
    Append a new section to `style.css` (after the existing "Reduced motion" block, before EOF) that declares CSS classes for the content-entry patterns used by Wave-2 plans. Use ONLY existing CSS custom properties (`--accent`, `--text`, `--text-strong`, `--text-muted`, `--border`, `--card-bg`, spacing tokens `--space-*`, radii `--radius-*`). Do NOT introduce any new color values, font sizes outside the established scale (15px/17px/22px/28px desktop, 14px/16px mobile), or font weights other than 400. Do NOT introduce hover-only animations or transitions (Carlini-minimal contract).

    Add a section comment header `/* ------------------------------------------------------------ Content-entry patterns (Phase 2: publications / talks / writing) ------------------------------------------------------------ */` then declare:

    1. **`.pub-entry`** — block-level container for one publication. Margin-block-end of `var(--space-8)` to give papers air. Border-bottom of `1px solid var(--border)` with `padding-bottom: var(--space-6)` to visually separate adjacent papers (subtle hairline, matches news-entry treatment). On `:last-child` strip the border-bottom and padding-bottom. Inside, declare:
       - `.pub-entry .pub-title` — paper title block. font-size: 17px desktop / 16px mobile (use existing media query `@media (max-width: 639px)`). color: `var(--text-strong)`. line-height: 1.4. margin: 0 0 var(--space-2) 0. font-weight: 400 (single weight invariant).
       - `.pub-entry .pub-venue` — authors + venue line. font-size: 15px. color: `var(--text-muted)`. line-height: 1.5. margin: 0 0 var(--space-3) 0. The `<i>` inside (italicized venue) inherits font-style: italic from browser default — do not override.
       - `.pub-entry .pub-links` — row of resource links (Paper, Code, Slides, etc.). font-size: 15px. margin: 0. Children separated by a `.pub-sep` span (pipe `|`) styled with `color: var(--text-muted); margin: 0 var(--space-2);`. Reuses the same pipe-separator visual pattern as `.profile-card .profile-links .sep`.
       - `.pub-entry .pub-tldr` — optional short TLDR/summary paragraph. font-size: 15px. color: `var(--text)`. line-height: 1.55. margin: var(--space-2) 0 0 0. font-style: italic (signals it's a paraphrased TLDR, distinct from body prose). The TLDR is folded inline (no collapsible toggle — see Plan 02-02 for rationale).

    2. **`.talk-entry`** — block-level container for one talk. Same margin/border treatment as `.pub-entry` (`margin-block-end: var(--space-6); padding-bottom: var(--space-4); border-bottom: 1px solid var(--border);` and `:last-child` reset). Inside:
       - `.talk-entry .talk-body` — title + venue + year + links rendered as a single flowing paragraph. font-size: 17px desktop / 16px mobile. color: `var(--text)`. line-height: 1.6. margin: 0.

    3. **`.writing-year-group`** — wraps each year's entries. margin-block-end: `var(--space-10)`. On `:last-child` strip the margin.
       - `.writing-year-group h3` — the year heading (e.g. "2023"). font-size: 17px (same as body — the year is a quiet sectional marker, not a hero). letter-spacing: 0.04em. font-variant: small-caps (matches site-wide heading convention). color: `var(--text-strong)`. margin: 0 0 var(--space-4) 0. **Add to the existing global `h1, h2, h3` rule? No.** That rule already covers `h3` with the right small-caps and color tokens — but it sets `line-height: 1.2` and we want the year heading to integrate smoothly with the entry list below. Override only `margin` and `font-size` here; let the inherited small-caps + color flow through.
    4. **`.writing-entry`** — block-level container for one writing entry. margin-block-end: var(--space-6). On `:last-child` strip the margin. Inside:
       - `.writing-entry .writing-body` — link + colon + description rendered as a single paragraph. font-size: 17px desktop / 16px mobile. color: `var(--text)`. line-height: 1.6. margin: 0. The `<a>` inside inherits the global teal-underline link style — do not override.

    5. **`.code-intro`** — the short paragraph on `code.html`. font-size: 17px desktop / 16px mobile. color: `var(--text)`. line-height: 1.6. margin: 0. Optional: `max-width: 60ch` so the prose doesn't run the full 1100px width (matches readability conventions for body prose). The `<a>` inside inherits teal-underline.

    Mobile font-size adjustments (16px instead of 17px) MUST be scoped inside a single `@media (max-width: 639px)` block at the end of this section to keep the cascade flat. Do not duplicate media-query openings — group all 16px overrides into one block.

    Do NOT add: hover transitions, animation, gradient backgrounds, box-shadows, large background fills using `--accent`, or any selector that would over-specify and force later overrides. Keep each rule to 1-3 declarations where possible.

    Final byte: the file must end with a single trailing newline. Do not leave a trailing comment without a newline.
  </action>
  <verify>
    <automated>
      cd /Users/ivolinengong/Documents/My_Website &amp;&amp;
      grep -c '^\.pub-entry' style.css | awk '$1 &gt;= 1 {print "ok"}' | grep -q ok &amp;&amp;
      grep -q '\.pub-entry \.pub-title' style.css &amp;&amp;
      grep -q '\.pub-entry \.pub-venue' style.css &amp;&amp;
      grep -q '\.pub-entry \.pub-links' style.css &amp;&amp;
      grep -q '\.pub-entry \.pub-tldr' style.css &amp;&amp;
      grep -q '^\.talk-entry' style.css &amp;&amp;
      grep -q '\.writing-year-group' style.css &amp;&amp;
      grep -q '^\.writing-entry' style.css &amp;&amp;
      grep -q '\.code-intro' style.css &amp;&amp;
      # No new color hex outside the established token set — reject any newly-introduced hex
      # The existing palette is: #FFFFFF #1F2937 #111827 #6B7280 #0F766E #E5E7EB #FAFAFA #D1D5DB
      # Allow only those + 0-3-letter shorthand greys. Quick heuristic: count any 6-digit hex not in allowlist.
      ! grep -E '#[0-9A-Fa-f]{6}' style.css | grep -vE '#FFFFFF|#1F2937|#111827|#6B7280|#0F766E|#E5E7EB|#FAFAFA|#D1D5DB' | grep -E '#[0-9A-Fa-f]{6}' &amp;&amp;
      # No font-weight other than 400
      ! grep -E 'font-weight:\s*(bold|[5-9]00|100|200|300)' style.css &amp;&amp;
      echo "PASS: content-entry classes added with palette + weight invariants preserved"
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] `.pub-entry`, `.pub-entry .pub-title`, `.pub-entry .pub-venue`, `.pub-entry .pub-links`, `.pub-entry .pub-tldr` selectors all present in style.css
    - [ ] `.talk-entry` and `.talk-entry .talk-body` selectors present
    - [ ] `.writing-year-group`, `.writing-year-group h3`, `.writing-entry`, `.writing-entry .writing-body` selectors present
    - [ ] `.code-intro` selector present
    - [ ] All declarations use only existing custom properties (`--accent`, `--text`, `--text-strong`, `--text-muted`, `--border`, `--card-bg`, `--space-*`, `--radius-*`). No new hex values introduced (palette grep gate above)
    - [ ] No `font-weight` value other than 400 anywhere in the file
    - [ ] Mobile font-size variations consolidated into a single `@media (max-width: 639px)` block within the new section
    - [ ] File ends with a single trailing newline
    - [ ] Existing rules (`.site-header`, `.site-nav`, `.bio-layout`, `.news-list`, `a`, etc.) unchanged — verified by visual diff in commit
  </acceptance_criteria>
  <done>style.css now declares the 5 content-entry class families (.pub-entry, .talk-entry, .writing-year-group, .writing-entry, .code-intro) so that Wave-2 plans can render their pages without inventing new CSS or violating the type/color invariants from Phase 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← static HTML/CSS files | Visitor's browser reads static files; no input crosses any trust boundary (no forms, no JS, no DB). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | Nav anchors in index.html | accept | Internal same-origin relative hrefs; no remote content loaded. |
| T-02-01-02 | Information Disclosure | New CSS classes | accept | CSS contains no secrets; same public-static surface as Phase 1. |
| T-02-01-03 | Spoofing (tabnabbing) | New anchors | mitigate | This plan introduces only same-origin `href` links with NO `target="_blank"` — no tabnabbing surface; verified by automated check in Task 1. |
| T-02-01-04 | Repudiation | All changes | accept | Static personal site, no auth. |
| T-02-01-05 | Denial of Service | CSS additions | accept | Hand-written CSS, no @import, no remote resources; appended ruleset is &lt; 60 lines. |
| T-02-01-06 | Elevation of Privilege | n/a | accept | No code paths exist. |
| T-02-01-SC | Tampering | package installs | n/a | Zero packages installed in Phase 2 (no npm/pip/cargo); no package legitimacy gate required. |
</threat_model>

<verification>
Run from repo root after both tasks complete:

```bash
cd /Users/ivolinengong/Documents/My_Website
# Nav activation
grep -c 'data-todo="phase-2"' index.html | grep -q '^0$' || echo FAIL: placeholders remain
grep -q 'href="publications.html">Papers</a>' index.html || echo FAIL: Papers anchor missing
grep -q 'href="talks.html">Talks</a>' index.html || echo FAIL: Talks anchor missing
grep -q 'href="code.html">Code</a>' index.html || echo FAIL: Code anchor missing
grep -q 'href="writing.html">Writing</a>' index.html || echo FAIL: Writing anchor missing
grep -q '<span aria-current="page">Main</span>' index.html || echo FAIL: Main marker removed
! grep -q 'target="_blank"' index.html || echo FAIL: target=_blank introduced

# CSS additions
for sel in '\.pub-entry' '\.pub-entry \.pub-title' '\.pub-entry \.pub-venue' '\.pub-entry \.pub-links' '\.pub-entry \.pub-tldr' '\.talk-entry' '\.talk-entry \.talk-body' '\.writing-year-group' '\.writing-entry' '\.writing-entry \.writing-body' '\.code-intro'; do
  grep -qE "$sel" style.css || echo "FAIL: selector $sel missing"
done
! grep -E 'font-weight:\s*(bold|[5-9]00|100|200|300)' style.css || echo FAIL: non-400 weight introduced
echo "VERIFICATION COMPLETE"
```
</verification>

<success_criteria>
1. `grep -c 'data-todo="phase-2"' index.html` returns 0
2. All four Phase-2 anchors (`publications.html`, `talks.html`, `code.html`, `writing.html`) are present in index.html
3. `index.html`'s Main item still carries `aria-current="page"` and is not a self-link
4. style.css contains all 11 content-entry selectors enumerated in Task 2 acceptance criteria
5. No new hex colors and no font-weights other than 400 introduced in style.css
6. Loading `file:///Users/ivolinengong/Documents/My_Website/index.html` shows 5 teal nav items in order Main / Papers / Talks / Code / Writing (Wave-2 plans will populate the destination pages — clicking Papers will currently 404 until Plan 02-02 lands, which is expected during execution)
</success_criteria>

<output>
Create `.planning/phases/02-remaining-content-pages/02-01-SUMMARY.md` documenting:
- The exact lines edited in index.html (line-by-line diff summary)
- The new CSS rules added to style.css (selector list + total lines added)
- Confirmation that the nav activation did NOT regress any Phase-1 invariant (no target=_blank, no removed Wayback prefixes, news-list intact, profile-card intact)
- Any deviations from the plan
</output>
