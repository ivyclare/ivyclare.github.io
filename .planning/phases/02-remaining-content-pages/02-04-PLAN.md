---
phase: 02-remaining-content-pages
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - writing.html
autonomous: true
requirements:
  - CONT-07
  - PAGE-04
  - PAGE-06
tags:
  - static-site
  - content-extraction
  - writing-page
must_haves:
  truths:
    - "A visitor opening writing.html sees a page-title `<h2>Writing</h2>` and below it 4 year-grouped sections (2023, 2022, 2021, 2019) containing 6 writing entries in total, extracted verbatim from `reference/wayback/Writing - Ivoline Ngong.html`."
    - "Each entry renders as a single paragraph: a teal-underlined link to the canonical post URL, followed by a short verbatim description."
    - "The shared site-header + site-nav appears at the top, with the `Writing` nav item carrying `aria-current=\"page\"`."
    - "All recovered URLs are canonical (no `web.archive.org` prefix)."
    - "Note: 3 of the 6 entries have broken WordPress-placeholder URLs in the wayback source (`/748-2/` on Comic PATE 2021, `/hello-world/` on both 2019 entries). These are flagged broken in 02-04-SUMMARY per CONT-09 and the anchors are dropped (the description text is kept). This matches Phase 1's broken-anchor-drop pattern (D-04 in 01-03-SUMMARY)."
  artifacts:
    - path: "writing.html"
      provides: "Writing page — blog post listing grouped by year"
      min_lines: 50
      contains: "<h2>Writing</h2>"
  key_links:
    - from: "writing.html nav"
      to: "index.html / publications.html / talks.html / code.html"
      via: '<a href> elements in <nav class="site-nav">'
      pattern: 'site-nav.*href="(index|publications|talks|code)\.html"'
---

# Phase 2 Plan 04 — writing.html (Writing listing)

## Phase Goal

**As a** visitor to ivolinengong.com, **I want to** open `writing.html` and see a year-grouped list of Ivoline's blog posts with one-line descriptions, **so that** I can skim the topics and click through to read the full posts.

<objective>
Create `writing.html` at repo root — a single static HTML file rendering the 6 writing entries listed on the wayback Writing page, grouped by year (2023, 2022, 2021, 2019), extracted verbatim and rendered on the shared site shell with Writing marked as the current nav item.

Purpose: Phase 2 deliverable. The wayback Writing page presents entries grouped under `<h2>` year headings — this plan re-expresses that structure using `<h3>` year sub-headings (the page-level `<h2>` is reserved for `Writing` per the site convention) and the `.writing-year-group` + `.writing-entry` classes from Plan 02-01.

Output: 1 new file `writing.html` at the repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-home-mvp/01-UI-SPEC.md
@index.html
@style.css
@reference/wayback/Writing - Ivoline Ngong.html

<interfaces>
<!-- The 6 entries in wayback source order (lines 684-745), already with Wayback prefixes stripped. -->
<!-- Note: heading level shift — the wayback source uses <h2> for year headings (2023, 2022, 2021, 2019). -->
<!-- We promote `Writing` (the page-level heading) to <h2> and demote year headings to <h3> so the document outline stays h2 → h3. -->
<!-- This matches UI-SPEC §Typography (h2 = section heading; h3 = sub-section) and the existing style.css h1,h2,h3 rule. -->

Year-grouped contents (re-verify in source before writing):

**2023** (wayback lines 684, 689):
- *How To Audit An AI Model Owned by Someone else (Part 1)* — `https://blog.openmined.org/ai-audit-part-1/` — description: "Many AI systems today lack comprehensive external evaluation in terms of their value alignment, fairness, safety, and other critical aspects. The few external entities that have access are often granted excessive permissions, leading to potential privacy, security, and intellectual property risks. Current access methods struggle to balance auditor access with AI owner risks. This blogpost series presents a new approach using PySyft "Domain" servers. The system enables auditors to ask a specific question about an AI system. If all parties approve the question, the auditor receives the answer without exposing any additional information, ensuring a more focused and secure audit process."

**2022** (wayback line 705):
- *Prediction Sensitivity: Continual Audit of Counterfactual Fairness in Deployed Classifiers* — `https://montrealethics.ai/prediction-sensitivity-continual-audit-of-counterfactual-fairness-in-deployed-classifiers/` — description: "We present prediction sensitivity, an approach for auditing counterfactual fairness while the model is deployed and making predictions."

**2021** (wayback lines 721, 725):
- *Maintaining Privacy in Medical Data with Differential Privacy* — `https://blog.openmined.org/maintaining-privacy-in-medical-data-with-differential-privacy/` — description: "How can you make use of these datasets without accessing them directly? How can you assure these hospitals that their patients' data will be protected? Is it even a possibility? We try to answer these questions in this blog post."
- *Comic – PATE Analysis* — `https://ivolinengong.com/748-2/` — description: "A comic representing the PATE Framework."
  - ⚠ The wayback URL `https://ivolinengong.com/748-2/` is a self-reference to a WordPress slug on the now-dead domain. Per Phase 1 protocol (01-03-SUMMARY D-04: "news-broken-links"), self-referential `ivolinengong.com/...` URLs are dropped and the text is retained. **Drop the `<a>` wrapping** and render the entry as `Comic – PATE Analysis: A comic representing the PATE Framework.` (no link). Flag in SUMMARY.

**2019** (wayback lines 741, 745):
- *Deep CARs— Transfer Learning With Pytorch* — `https://ivolinengong.com/hello-world/` — description: "How can you teach a computer to recognize different car brands? Would you like to take a picture of any car and your phone automatically tells you what the make of the car is?"
  - ⚠ The wayback URL is `https://ivolinengong.com/hello-world/` — a WordPress default placeholder URL. Per Phase 1 D-04, drop the `<a>` wrapping. Render text-only. Flag in SUMMARY.
- *Population vs Sample , Statistic vs Parameter* — `https://ivolinengong.com/hello-world/` — description: "Basics of descriptive statistics."
  - ⚠ Same `hello-world` placeholder URL — drop the `<a>` wrapping. Render text-only. Flag in SUMMARY.

Per-entry markup contract (uses `.writing-entry` from Plan 02-01):

WITH link:
```html
<article class="writing-entry">
  <p class="writing-body"><a href="CANONICAL-URL">TITLE:</a> DESCRIPTION</p>
</article>
```

WITHOUT link (broken self-ref or hello-world placeholder dropped):
```html
<article class="writing-entry">
  <p class="writing-body">TITLE: DESCRIPTION</p>
</article>
```

Year-group wrapper:
```html
<section class="writing-year-group">
  <h3>YEAR</h3>
  <article class="writing-entry">...</article>
  <article class="writing-entry">...</article>
</section>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write writing.html (shell + nav + 4 year-groups + 6 entries) in a single pass</name>
  <files>writing.html</files>
  <read_first>
    - /Users/ivolinengong/Documents/My_Website/index.html (verbatim source of the site shell)
    - /Users/ivolinengong/Documents/My_Website/reference/wayback/Writing - Ivoline Ngong.html (PRIMARY SOURCE — read lines 670-755 for the 6 entries grouped by year)
    - /Users/ivolinengong/Documents/My_Website/.planning/phases/01-skeleton-home-mvp/01-UI-SPEC.md (Copywriting Contract — confirms title format)
    - /Users/ivolinengong/Documents/My_Website/.planning/phases/01-skeleton-home-mvp/01-03-SUMMARY.md (`decisions.bio-broken-links` and `decisions.news-broken-links` — pattern for handling `ivolinengong.com/hello-world` and other broken placeholder URLs)
    - /Users/ivolinengong/Documents/My_Website/.planning/phases/02-remaining-content-pages/02-01-PLAN.md (`<interfaces>` section — confirms `.writing-year-group`, `.writing-entry`, `.writing-body` class contract)
  </read_first>
  <action>
    Create `/Users/ivolinengong/Documents/My_Website/writing.html` containing the full page in one pass (shell + 4 year sections + 6 entries).

    **1. Site shell** — identical to publications.html / talks.html scaffolding, with these deltas:
       - `<title>Writing — Ivoline Ngong</title>`
       - `<meta name="description" content="Writing by Ivoline Ngong — blog posts on differential privacy, fairness audits, and AI.">` (end with a period)
       - In nav, `Writing` is the `<span aria-current="page">`. Final nav order:
         - `<a href="index.html">Main</a>`
         - `<a href="publications.html">Papers</a>`
         - `<a href="talks.html">Talks</a>`
         - `<a href="code.html">Code</a>`
         - `<span aria-current="page">Writing</span>`
       - `<a class="site-title" href="index.html">Ivoline Ngong</a>` — NO `aria-current`.

    **2. Page heading** — `<h2>Writing</h2>` is the first child of `<main id="main">`.

    **3. Year groups** — emit 4 `<section class="writing-year-group">` blocks in wayback source order (2023, 2022, 2021, 2019). Each has an `<h3>YEAR</h3>` heading followed by 1 or 2 `<article class="writing-entry">` blocks.

    **2023 group** — 1 entry:
    ```html
    <section class="writing-year-group">
      <h3>2023</h3>
      <article class="writing-entry">
        <p class="writing-body"><a href="https://blog.openmined.org/ai-audit-part-1/">How To Audit An AI Model Owned by Someone else (Part 1)</a> Many AI systems today lack comprehensive external evaluation in terms of their value alignment, fairness, safety, and other critical aspects. The few external entities that have access are often granted excessive permissions, leading to potential privacy, security, and intellectual property risks. Current access methods struggle to balance auditor access with AI owner risks. This blogpost series presents a new approach using PySyft “Domain” servers. The system enables auditors to ask a specific question about an AI system. If all parties approve the question, the auditor receives the answer without exposing any additional information, ensuring a more focused and secure audit process.</p>
      </article>
    </section>
    ```

    Note for 2023 entry: the wayback source line 689 ends the link text with `&nbsp;` (visible in the file). Strip that trailing `&nbsp;` from the link text itself, but the space between the link and the description is naturally preserved by the prose flow. Also note: the linking convention used by the wayback source is **link-then-prose** with no colon separator — title is the link text, description is the following prose. Preserve this. (Title text does NOT include a trailing colon.)

    **2022 group** — 1 entry:
    ```html
    <section class="writing-year-group">
      <h3>2022</h3>
      <article class="writing-entry">
        <p class="writing-body"><a href="https://montrealethics.ai/prediction-sensitivity-continual-audit-of-counterfactual-fairness-in-deployed-classifiers/">Prediction Sensitivity: Continual Audit of Counterfactual Fairness in Deployed Classifiers:</a> We present prediction sensitivity, an approach for auditing counterfactual fairness while the model is deployed and making predictions.</p>
      </article>
    </section>
    ```

    Note: the 2022 entry's wayback link text ENDS in `:` (the title includes a trailing colon as part of the link text per line 705). Preserve the colon inside the `<a>` element. Then `&nbsp;` follows the closing `</a>` — preserve as a literal `&nbsp;` for spacing fidelity (or render as a regular space; either is acceptable — pick `&nbsp;` for source fidelity).

    **2021 group** — 2 entries (the second one has its anchor dropped per the broken-link rule):
    ```html
    <section class="writing-year-group">
      <h3>2021</h3>
      <article class="writing-entry">
        <p class="writing-body"><a href="https://blog.openmined.org/maintaining-privacy-in-medical-data-with-differential-privacy/">Maintaining Privacy in Medical Data with Differential Privacy:</a> How can you make use of these datasets without accessing them directly? How can you assure these hospitals that their patients’ data will be protected? Is it even a possibility? We try to answer these questions in this blog post.</p>
      </article>
      <article class="writing-entry">
        <p class="writing-body">Comic – PATE Analysis: A comic representing the PATE Framework.</p>
      </article>
    </section>
    ```

    Note for Comic entry: the wayback source links to `https://ivolinengong.com/748-2/` (a dead WordPress slug). Drop the `<a>` wrapping. Render TITLE-only as plain text (no anchor). The em-dash `–` (U+2013) in "Comic – PATE Analysis" is preserved verbatim; do NOT replace with ASCII hyphen.

    **2019 group** — 2 entries (both have anchors dropped — both point at `hello-world` placeholder):
    ```html
    <section class="writing-year-group">
      <h3>2019</h3>
      <article class="writing-entry">
        <p class="writing-body">Deep CARs— Transfer Learning With Pytorch: How can you teach a computer to recognize different car brands? Would you like to take a picture of any car and your phone automatically tells you what the make of the car is?</p>
      </article>
      <article class="writing-entry">
        <p class="writing-body">Population vs Sample , Statistic vs Parameter: Basics of descriptive statistics.</p>
      </article>
    </section>
    ```

    Note for 2019 entries:
    - "Deep CARs— Transfer Learning With Pytorch" uses an em-dash glued to "CARs" (no space before the em-dash). PRESERVE verbatim. The wayback source has the title ending with `:` after "Pytorch" — preserve.
    - "Population vs Sample , Statistic vs Parameter" has a space BEFORE the comma (source-text quirk). PRESERVE verbatim per Phase 1 D-typo-preservation rule.

    **4. Extraction invariants** (Phase 1 protocol):
       - Strip every `https://web.archive.org/web/{14-digit-timestamp}/` prefix.
       - DROP inline styles like `style="color: #009688;"` and `style="background-color: #ffffff; color: #009688;"` from every `<a>` — the global `a { color: var(--accent); }` rule handles coloring (DES-01).
       - DROP `target="_blank"` and `rel="noopener"` from every link.
       - DROP the entire `<a>` wrapping for entries whose canonical URL is `https://ivolinengong.com/748-2/` (self-ref WordPress slug) or `https://ivolinengong.com/hello-world/` (WordPress default placeholder). Keep the entry's TITLE and DESCRIPTION text inline as plain prose. Per CONT-09 the SUMMARY must flag these 3 entries.
       - PRESERVE curly quotes (`“ ” ‘ ’`) and HTML entities (`&amp;`, `&nbsp;`) verbatim where the wayback source has them.
       - PRESERVE source-text typos verbatim (the "Population vs Sample , Statistic vs Parameter" space-before-comma is intentional preservation).
       - PRESERVE entry order within each year (wayback source order).
       - PRESERVE year-group order: 2023 first, then 2022, then 2021, then 2019.

    **5. Page closing**: close `</main></body></html>` with a single trailing newline. NO `<footer>`, NO `<script>`, NO inline `<style>`.

    Indentation: 2 spaces top-level, 4 spaces inside `<main>`, 6 spaces inside each `<section class="writing-year-group">` for `<h3>` and `<article>` children.
  </action>
  <verify>
    <automated>
      cd /Users/ivolinengong/Documents/My_Website &amp;&amp;
      test -f writing.html &amp;&amp;
      grep -iq '<!doctype html>' writing.html &amp;&amp;
      grep -q '<title>Writing — Ivoline Ngong</title>' writing.html &amp;&amp;
      grep -q 'href="style.css"' writing.html &amp;&amp;
      grep -q '<a class="site-title" href="index.html">Ivoline Ngong</a>' writing.html &amp;&amp;
      grep -q '<a href="index.html">Main</a>' writing.html &amp;&amp;
      grep -q '<a href="publications.html">Papers</a>' writing.html &amp;&amp;
      grep -q '<a href="talks.html">Talks</a>' writing.html &amp;&amp;
      grep -q '<a href="code.html">Code</a>' writing.html &amp;&amp;
      grep -q '<span aria-current="page">Writing</span>' writing.html &amp;&amp;
      grep -q '<h2>Writing</h2>' writing.html &amp;&amp;
      [ "$(grep -c '<section class="writing-year-group">' writing.html)" = "4" ] &amp;&amp;
      [ "$(grep -c '<article class="writing-entry">' writing.html)" = "6" ] &amp;&amp;
      [ "$(grep -c '<p class="writing-body">' writing.html)" = "6" ] &amp;&amp;
      grep -q '<h3>2023</h3>' writing.html &amp;&amp;
      grep -q '<h3>2022</h3>' writing.html &amp;&amp;
      grep -q '<h3>2021</h3>' writing.html &amp;&amp;
      grep -q '<h3>2019</h3>' writing.html &amp;&amp;
      # Year headings appear in declared order (2023 must come before 2019 in the file)
      [ "$(grep -n '<h3>' writing.html | head -1 | grep -o '2023' | wc -l)" = "1" ] &amp;&amp;
      [ "$(grep -n '<h3>' writing.html | tail -1 | grep -o '2019' | wc -l)" = "1" ] &amp;&amp;
      # Canonical URLs for the 2 entries that DO link
      grep -q 'blog.openmined.org/ai-audit-part-1' writing.html &amp;&amp;
      grep -q 'montrealethics.ai/prediction-sensitivity-continual-audit-of-counterfactual-fairness-in-deployed-classifiers' writing.html &amp;&amp;
      grep -q 'blog.openmined.org/maintaining-privacy-in-medical-data-with-differential-privacy' writing.html &amp;&amp;
      # Broken anchors dropped — these substrings must NOT appear in any href
      ! grep -qE 'href="[^"]*ivolinengong\.com/(hello-world|748-2)' writing.html &amp;&amp;
      # Content-text presence (broken-link entries kept their TEXT)
      grep -q 'Comic .* PATE Analysis' writing.html &amp;&amp;
      grep -q 'Deep CARs.*Transfer Learning With Pytorch' writing.html &amp;&amp;
      grep -q 'Population vs Sample' writing.html &amp;&amp;
      # Invariants
      [ "$(grep -c 'web.archive.org' writing.html)" = "0" ] &amp;&amp;
      [ "$(grep -c 'target="_blank"' writing.html)" = "0" ] &amp;&amp;
      [ "$(grep -c 'rel="noopener"' writing.html)" = "0" ] &amp;&amp;
      [ "$(grep -c '<script' writing.html)" = "0" ] &amp;&amp;
      [ "$(grep -c 'style="background-color' writing.html)" = "0" ] &amp;&amp;
      [ "$(grep -c 'style="color' writing.html)" = "0" ] &amp;&amp;
      ! grep -qE '<a[^>]*aria-current' writing.html &amp;&amp;
      [ "$(wc -l &lt; writing.html)" -ge "40" ] &amp;&amp;
      echo "PASS: writing.html written with 6 entries across 4 year-groups, broken anchors dropped"
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] `writing.html` exists at repo root
    - [ ] `<title>` is exactly `Writing — Ivoline Ngong`
    - [ ] Nav has 5 children in order Main / Papers / Talks / Code / Writing, with `Writing` as the only `<span aria-current="page">`
    - [ ] `<h2>Writing</h2>` is the first child of `<main id="main">`
    - [ ] Exactly 4 `<section class="writing-year-group">` elements
    - [ ] Each year section starts with `<h3>YEAR</h3>` for YEAR in {2023, 2022, 2021, 2019}
    - [ ] Year sections appear in order: 2023 first in the document, 2019 last
    - [ ] Exactly 6 `<article class="writing-entry">` elements distributed: 1 in 2023, 1 in 2022, 2 in 2021, 2 in 2019
    - [ ] Exactly 3 entries contain an `<a href>` (2023 → blog.openmined.org/ai-audit; 2022 → montrealethics.ai; first 2021 → blog.openmined.org/maintaining-privacy)
    - [ ] Exactly 3 entries are link-less prose (anchors dropped per CONT-09): the second 2021 entry "Comic – PATE Analysis"; both 2019 entries ("Deep CARs…" and "Population vs Sample, Statistic vs Parameter")
    - [ ] No `href="https://ivolinengong.com/hello-world"` or `href="https://ivolinengong.com/748-2"` strings (broken anchors dropped, text retained)
    - [ ] Zero `web.archive.org`, zero `target="_blank"`, zero `rel="noopener"`, zero `<script>`, zero `style="…"` inline styles
  </acceptance_criteria>
  <done>writing.html renders the wayback writing entries grouped by year using `.writing-year-group` + `.writing-entry`; broken WordPress-placeholder anchors dropped (text retained); the actual entry count is confirmed against the wayback source during execution.</done>
</task>

</tasks>

<!-- Entry count verified post-checker: 6 entries (1 in 2023 line 689, 1 in 2022 line 705, 2 in 2021 lines 721/725, 2 in 2019 lines 741/745). All instances of "5" updated to "6" across must_haves, action, acceptance_criteria, and automated verify gates. -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← writing.html | Static file; outbound links cross to blog.openmined.org and montrealethics.ai (3 active links). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Spoofing | External blog links | accept | Owner-curated canonical URLs. |
| T-02-04-02 | Tampering | Wayback-prefix injection | mitigate | Strip protocol; automated grep verifies zero `web.archive.org`. |
| T-02-04-03 | Spoofing (tabnabbing) | External links | mitigate | Zero `target="_blank"` (Phase 1 invariant). |
| T-02-04-04 | Tampering | Broken WordPress placeholder URLs | mitigate | `ivolinengong.com/hello-world` and `ivolinengong.com/748-2` self-refs are dropped (text-only entry, no anchor); flagged in SUMMARY per CONT-09. Prevents the page from generating misleading clickable links to dead destinations. |
| T-02-04-05 | Repudiation / Integrity | Verbatim content | mitigate | CONT-09: dropped anchors flagged in SUMMARY; no fabrication. |
| T-02-04-06 | Denial of Service | Page weight | accept | Small static HTML, no images. |
| T-02-04-SC | Tampering | package installs | n/a | Zero packages. |
</threat_model>

<verification>
End-of-plan verification:

```bash
cd /Users/ivolinengong/Documents/My_Website
test -f writing.html || echo FAIL: file missing
grep -q '<title>Writing — Ivoline Ngong</title>' writing.html || echo FAIL: title wrong
grep -q '<span aria-current="page">Writing</span>' writing.html || echo FAIL: Writing not marked current
[ "$(grep -c '<section class="writing-year-group">' writing.html)" = "4" ] || echo FAIL: wrong year-group count
# Entry count: re-verified against wayback source = 6 entries
[ "$(grep -c '<article class="writing-entry">' writing.html)" = "6" ] || echo FAIL: expected 6 entries
! grep -q 'web.archive.org' writing.html || echo FAIL: wayback prefix leaked
! grep -q 'target="_blank"' writing.html || echo FAIL: target=_blank introduced
! grep -qE 'href="[^"]*ivolinengong\.com/(hello-world|748-2)' writing.html || echo FAIL: broken-anchor leaked
! grep -q '<script' writing.html || echo FAIL: script tag introduced
echo "VERIFICATION COMPLETE"
```
</verification>

<success_criteria>
1. writing.html exists at repo root and opens cleanly as a local file
2. Shared site-header + nav renders with Writing marked as current page
3. `<h2>Writing</h2>` heading present
4. 4 year sections in order 2023 → 2022 → 2021 → 2019
5. All entries from the wayback source are present (executor confirms total count: planner estimate is 6 — re-verify in source)
6. The 3 active blog post links use canonical URLs (no `web.archive.org` prefix)
7. The 3 broken WordPress-placeholder URLs (`/748-2/` and 2× `/hello-world/`) are dropped — entries appear as text-only prose without a link
8. The page works with JavaScript disabled
9. Source-text quirks ("Population vs Sample , Statistic vs Parameter" space-before-comma; em-dash glued in "Deep CARs—") preserved verbatim
</success_criteria>

<output>
Create `.planning/phases/02-remaining-content-pages/02-04-SUMMARY.md` documenting:
- The N (planner estimate: 6; executor confirms actual) writing entries extracted in a table (year | title | canonical URL or "DROPPED (placeholder)" | description present yes/no)
- Each entry where the anchor was dropped due to a broken placeholder URL — list specifically:
  - 2021 Comic – PATE Analysis: original URL was `https://ivolinengong.com/748-2/` (WordPress slug self-ref). DROPPED.
  - 2019 Deep CARs— Transfer Learning With Pytorch: original URL was `https://ivolinengong.com/hello-world/`. DROPPED.
  - 2019 Population vs Sample , Statistic vs Parameter: original URL was `https://ivolinengong.com/hello-world/`. DROPPED.
- Owner-action recommendation: these 3 entries probably have real URLs elsewhere (e.g. Medium, openmined blog archives) — owner can supply the real URLs in a one-line follow-up.
- Source-text quirks preserved verbatim (space-before-comma; glued em-dash; etc.)
- Confirmation that no Wayback prefixes, no `target="_blank"`, no inline styles, no `<script>` tags leaked into the final HTML
- Threat-model dispositions honored
</output>
