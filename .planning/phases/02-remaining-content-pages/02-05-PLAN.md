---
phase: 02-remaining-content-pages
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - code.html
autonomous: true
requirements:
  - PAGE-05
  - PAGE-06
tags:
  - static-site
  - code-page
  - github-link
must_haves:
  truths:
    - "A visitor opening code.html sees a page-title `<h2>Code</h2>` and a short paragraph below it directing them to Ivoline's GitHub profile."
    - "The paragraph contains a teal-underlined `<a>` link with href `https://github.com/ivyclare` and visible text `github.com/ivyclare`."
    - "The shared site-header + site-nav appears at the top, with the `Code` nav item carrying `aria-current=\"page\"`."
    - "The page is intentionally short (under ~50 lines total) — no listing, no project entries, no auto-redirect script. Carlini-minimal: an explicit clickable link, not a JS redirect (per UI-SPEC §Copywriting Contract: 'explicit link, not auto-redirect — keeps the site inert and gracefully handles users opening with JS disabled')."
  artifacts:
    - path: "code.html"
      provides: "Code page — short pointer to the owner's GitHub profile"
      contains: 'href="https://github.com/ivyclare"'
  key_links:
    - from: "code.html nav"
      to: "index.html / publications.html / talks.html / writing.html"
      via: '<a href> elements in <nav class="site-nav">'
      pattern: 'site-nav.*href="(index|publications|talks|writing)\.html"'
    - from: "code.html body"
      to: "github.com/ivyclare"
      via: "single <a> in <p class='code-intro'>"
      pattern: 'href="https://github\.com/ivyclare"'
---

# Phase 2 Plan 05 — code.html (Code page → GitHub pointer)

## Phase Goal

**As a** visitor who clicked "Code" in the site nav expecting to see a project portfolio, **I want to** be told plainly that Ivoline's code lives on GitHub and given a single click-through link to her profile, **so that** I can find her repositories without confusion or wasted scrolling.

<objective>
Create `code.html` at repo root — a deliberately short static HTML file with the shared site shell and a single paragraph pointing at `github.com/ivyclare`. This is the smallest of the five Phase-2 deliverables; per UI-SPEC §Copywriting Contract it is a plain explicit link, not a JavaScript auto-redirect (so the page works fully with JS disabled and never appears to teleport).

Purpose: Phase 2 deliverable that closes the loop on the 5-page navigation set. The owner does not curate code on this site — they point visitors to GitHub. Keeping this page is preferable to deleting the Code nav item because (a) faithfulness to the original site's 5-item menu, and (b) gives the owner a future home if they later want to feature specific repos.

Output: 1 new file `code.html` at the repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-skeleton-home-mvp/01-UI-SPEC.md
@index.html
@style.css

<interfaces>
<!-- UI-SPEC §Copywriting Contract row "Code page body (when built)" specifies the body copy verbatim: -->
<!-- "My code lives on GitHub: <a href="https://github.com/ivyclare">github.com/ivyclare</a>" -->
<!-- Decision rationale (UI-SPEC): "explicit link, not auto-redirect — keeps the site inert -->
<!-- and gracefully handles users opening with JS disabled" -->

The page uses the `.code-intro` class declared in Plan 02-01 to constrain the paragraph's max-width and apply the standard body type style.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write code.html (full page in one pass)</name>
  <files>code.html</files>
  <read_first>
    - /Users/ivolinengong/Documents/My_Website/index.html (verbatim source of the site shell)
    - /Users/ivolinengong/Documents/My_Website/.planning/phases/01-skeleton-home-mvp/01-UI-SPEC.md (§Copywriting Contract row "Code page body (when built)" — the exact body sentence; §Site header — confirms the nav active-marker placement)
    - /Users/ivolinengong/Documents/My_Website/.planning/phases/02-remaining-content-pages/02-01-PLAN.md (`<interfaces>` section — confirms `.code-intro` class contract)
  </read_first>
  <action>
    Create `/Users/ivolinengong/Documents/My_Website/code.html` containing the full page in one pass.

    **1. Site shell** — identical to other Phase-2 pages, with these deltas:
       - `<title>Code — Ivoline Ngong</title>`
       - `<meta name="description" content="Ivoline Ngong's code lives on GitHub at github.com/ivyclare.">` (end with a period)
       - In nav, `Code` is the `<span aria-current="page">`. Final nav order:
         - `<a href="index.html">Main</a>`
         - `<a href="publications.html">Papers</a>`
         - `<a href="talks.html">Talks</a>`
         - `<span aria-current="page">Code</span>`
         - `<a href="writing.html">Writing</a>`
       - `<a class="site-title" href="index.html">Ivoline Ngong</a>` — NO `aria-current`.

    **2. Page heading** — `<h2>Code</h2>` as the first child of `<main id="main">`.

    **3. Page body** — a single `<p class="code-intro">` element immediately after the heading, containing EXACTLY this content (sourced from UI-SPEC §Copywriting Contract):

       ```html
       <p class="code-intro">My code lives on GitHub: <a href="https://github.com/ivyclare">github.com/ivyclare</a></p>
       ```

       That is the entire body. No `<ul>`, no project list, no "selected projects" subheading. The page is intentionally a one-liner.

    **4. Page closing**: close `</main></body></html>` with a single trailing newline.

    **5. Invariants**:
       - NO `<script>` (even a tiny redirect script — UI-SPEC explicitly forbids auto-redirect: "explicit link, not auto-redirect")
       - NO `<meta http-equiv="refresh">` (same reason as above — would auto-redirect)
       - NO `target="_blank"` on the GitHub link (Phase 1 invariant)
       - NO inline `<style>`
       - NO favicon link (Phase 3 polish)
       - The body link uses the canonical `https://github.com/ivyclare` (https, with the bare profile path; no `?tab=…` parameters)

    Indentation: 2 spaces top-level, 4 spaces inside `<main>`. The whole file should fit in ~25-30 lines.
  </action>
  <verify>
    <automated>
      cd /Users/ivolinengong/Documents/My_Website &amp;&amp;
      test -f code.html &amp;&amp;
      grep -iq '<!doctype html>' code.html &amp;&amp;
      grep -q '<title>Code — Ivoline Ngong</title>' code.html &amp;&amp;
      grep -q 'href="style.css"' code.html &amp;&amp;
      grep -q 'class="skip-link"' code.html &amp;&amp;
      grep -q '<a class="site-title" href="index.html">Ivoline Ngong</a>' code.html &amp;&amp;
      grep -q '<a href="index.html">Main</a>' code.html &amp;&amp;
      grep -q '<a href="publications.html">Papers</a>' code.html &amp;&amp;
      grep -q '<a href="talks.html">Talks</a>' code.html &amp;&amp;
      grep -q '<span aria-current="page">Code</span>' code.html &amp;&amp;
      grep -q '<a href="writing.html">Writing</a>' code.html &amp;&amp;
      grep -q '<h2>Code</h2>' code.html &amp;&amp;
      grep -q '<p class="code-intro">My code lives on GitHub: <a href="https://github.com/ivyclare">github.com/ivyclare</a></p>' code.html &amp;&amp;
      # Invariants
      [ "$(grep -c '<script' code.html)" = "0" ] &amp;&amp;
      [ "$(grep -c 'http-equiv="refresh"' code.html)" = "0" ] &amp;&amp;
      [ "$(grep -c 'target="_blank"' code.html)" = "0" ] &amp;&amp;
      [ "$(grep -c 'web.archive.org' code.html)" = "0" ] &amp;&amp;
      ! grep -qE '<a[^>]*aria-current' code.html &amp;&amp;
      # Size sanity: deliberately small (one-liner body)
      [ "$(wc -l &lt; code.html)" -le "40" ] &amp;&amp;
      [ "$(wc -l &lt; code.html)" -ge "15" ] &amp;&amp;
      echo "PASS: code.html written, deliberately small, no redirect mechanisms"
    </automated>
  </verify>
  <acceptance_criteria>
    - [ ] `code.html` exists at repo root
    - [ ] `<title>` is exactly `Code — Ivoline Ngong`
    - [ ] Nav has 5 children in order Main / Papers / Talks / Code / Writing, with `Code` as the only `<span aria-current="page">`
    - [ ] `<h2>Code</h2>` is the first child of `<main id="main">`
    - [ ] Body contains EXACTLY one `<p class="code-intro">` with the literal UI-SPEC content: `My code lives on GitHub: <a href="https://github.com/ivyclare">github.com/ivyclare</a>`
    - [ ] Zero `<script>` tags
    - [ ] Zero `<meta http-equiv="refresh">` (no auto-redirect mechanism)
    - [ ] Zero `target="_blank"`
    - [ ] Total file length is 15-40 lines (deliberately small)
  </acceptance_criteria>
  <done>code.html exists with the shared site shell, Code marked as the current page, and the verbatim UI-SPEC body sentence pointing at github.com/ivyclare.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ← code.html | Static file; one outbound link to github.com. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Spoofing | GitHub link | accept | Single owner-controlled canonical URL `https://github.com/ivyclare`. |
| T-02-05-02 | Spoofing (tabnabbing) | GitHub link | mitigate | No `target="_blank"` → no tabnabbing. |
| T-02-05-03 | Tampering | Auto-redirect | mitigate | UI-SPEC forbids auto-redirect (`<meta http-equiv="refresh">` or JS); automated grep gate verifies neither is present. Keeps the page inert and works with JS disabled. |
| T-02-05-04 | Repudiation / Integrity | Body content | mitigate | Body sentence is the verbatim UI-SPEC copywriting contract; no fabrication. |
| T-02-05-05 | Denial of Service | Page weight | accept | ~20-line static HTML; trivial cost. |
| T-02-05-SC | Tampering | package installs | n/a | Zero packages. |
</threat_model>

<verification>
End-of-plan verification:

```bash
cd /Users/ivolinengong/Documents/My_Website
test -f code.html || echo FAIL: file missing
grep -q '<title>Code — Ivoline Ngong</title>' code.html || echo FAIL: title wrong
grep -q '<span aria-current="page">Code</span>' code.html || echo FAIL: Code not marked current
grep -q '<p class="code-intro">My code lives on GitHub: <a href="https://github.com/ivyclare">github.com/ivyclare</a></p>' code.html || echo FAIL: body sentence wrong
! grep -q '<script' code.html || echo FAIL: script tag introduced
! grep -q 'http-equiv="refresh"' code.html || echo FAIL: meta-refresh introduced
! grep -q 'target="_blank"' code.html || echo FAIL: target=_blank introduced
echo "VERIFICATION COMPLETE"
```
</verification>

<success_criteria>
1. code.html exists at repo root and opens cleanly as a local file
2. Shared site-header + nav renders with Code marked as the current page
3. `<h2>Code</h2>` heading + single `<p class="code-intro">` paragraph
4. The paragraph contains the verbatim UI-SPEC sentence with a working anchor to `https://github.com/ivyclare`
5. The page does NOT auto-redirect (no JS, no meta-refresh) — visitor must explicitly click the link
6. The page works fully with JavaScript disabled
</success_criteria>

<output>
Create `.planning/phases/02-remaining-content-pages/02-05-SUMMARY.md` documenting:
- Confirmation that the body sentence matches UI-SPEC §Copywriting Contract verbatim
- Confirmation that no auto-redirect mechanism (script or meta-refresh) was introduced
- Confirmation that the 5-page nav set is now complete (Main, Papers, Talks, Code, Writing all exist as siblings; cross-page navigation works in any order without 404)
- Threat-model dispositions honored
</output>
